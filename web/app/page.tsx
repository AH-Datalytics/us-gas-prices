export default function HomePage() {
  return (
    <div className="flex items-center justify-center" style={{ minHeight: "calc(100vh - 60px)" }}>
      <p style={{ fontSize: 14, color: "var(--blue-mid)" }}>Loading...</p>
    </div>
  );
}
